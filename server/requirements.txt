# Core FastAPI backend — what every install needs.
# Python 3.9+ compatible (system Python on macOS is 3.9; DDSP needs 3.10
# but only matters when you opt into requirements-ddsp.txt).
#
# The app is designed to run without DDSP: /health returns
#   {"status":"degraded","ddsp_version":"unavailable"}
# and /synthesize + /fx/reverb return 503 with a clear error.
# Everything else (backing tracks, recording, scale playback, mobile bar,
# MIDI/MusicXML/Score21 export, practice sets) works fine on this core set.
#
# For the optional offline DDSP render + FFT reverb path, install
# requirements-ddsp.txt on top. That file pins Magenta/DDSP and pulls
# ~1.5 GB of TF + librosa.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12