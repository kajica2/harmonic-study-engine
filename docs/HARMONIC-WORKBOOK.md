# Harmonic Workbook — cross-reference

This file points at the separate `harmonic-workbook/` repo being built at:
`~/Documents/github-recovery/harmonic-workbook/`

That repo holds the M1–M7 PRD (`docs/PRD.md`) and will eventually contain a Vite/JS app using Verovio WASM for MusicXML rendering + Dexie storage + PDF export.

The parent engine (`main` at `599edc8` / `819f1eb`) is independent: React + TypeScript, 95 paths, 42 composers (after the catalog expansion), 784 tests passing.
