# Tech Debt Register

Initialized 2026-09-22.

| ID | Area | Description | Priority | Status |
|----|------|-------------|----------|--------|
| TD-001 | practice tally (take purity) | Record-flow `begin()` is idempotent when transport playing → pre-record notes fold into take (continuous-practice semantics). Documented in `App.tsx:1668`. | P3 | Closed — 2026-09-22, commit 25374b5 |
| TD-002 | practice tally (post-record stall) | After take `end()`, live tally immediately restarts via `guideTrail.begin()` if `isPlayingAutoRef.current` is true. | P3 | Closed — 2026-09-22, commit 25374b5 |
| TD-003 | practice tally (hygiene) | `useGuideToneTrail.ts` trailing newline added; effect deps use stable destructured callbacks. | P4 | Closed — 2026-09-22, commits 14b12de, 25374b5 |
| TD-004 | docs (stale wording) | Replaced "streak" with cumulative tally in `FUTURE_PLANNING.md`, `practiceHeader.ts`, and `docs/ARCHITECTURE.md`. | P3 | Closed — 2026-09-22, commit c816670 |
| TD-005 | coverage map (interval duplication) | Deduplicated interval table into exported `intervalToRole` and `roleToGuideToneTarget` in `guideTones.ts`, used by `gtTargets.ts`, pinned with 3 parity tests. | P3 | Closed — 2026-09-22, commit a5b7e88 |
| TD-006 | coverage map (hygiene) | Length-guard `console.warn` moved to `useEffect` in `PlaySessionRail.tsx`. | P4 | Closed — 2026-09-22, commit 14b12de |
| TD-007 | config (test discovery) | Renamed `tests/count-tunes.test.js` to `.ts` for automatic discovery by Vitest. | P3 | Closed — 2026-09-22, commit 9ea7b82 |
| TD-008 | docs (coverage row) | Resynced test counts across `SPEC.md`, `README.md`, `src/magenta/README.md`, and `docs/ARCHITECTURE.md` to 384 (355 frontend + 29 backend). | P3 | Closed — 2026-09-22, commit 9ea7b82 |
| TD-009 | midi input (channel splat) | Expose `channel` (1-16) on `MidiInEvent` + `midiOut` input fan-out. Backward-compatible signature on `onNoteOn` / `onNoteOff`. | P3 | Closed — 2026-09-22, commit 63d2ca5 |
| TD-010 | guide tones (channel filter) | `useGuideToneTrail(chordNotes, bassChannel)` drops bass-channel notes from the tally so a Stick / bass-pedal setup doesn't pollute guide-tone feedback. | P3 | Closed — 2026-09-22, commit 63d2ca5 |
| TD-011 | piano bass layer (merge) | `useBassNotes` merges `backingEngine` stream + live MIDI bass on the configured channel (default 2). Both subscriptions clean up on unmount. | P3 | Closed — 2026-09-22, commit 63d2ca5 |
| TD-012 | config (persisted bass channel) | `bassMidiChannel` via `usePersistedState` (`bassMidiChannel`, default 2). Header chip "BASS CH" selects 1-16. | P3 | Closed — 2026-09-22, commit 63d2ca5 |
| TD-013 | tests (channel split pins) | +12 pins across midiIn (3), useGuideToneTrail (2), useBassNotes (7). Total tests: 969 passed / 1 skipped (+70 vs pre-feature baseline 899 / 1). | P3 | Closed — 2026-09-22, commit 63d2ca5 |
| TD-014 | docs (channel-split spec) | `docs/midiChannelSplit-feature.md` follows `docs/midiClock-feature.md` structure. Data flow diagram included. | P3 | Closed — 2026-09-22, commit 63d2ca5 |
| TD-015 | engine (validator branch coverage) | `validateStyleProfile`: ~31 of 33 error branches untested (registers, enums, tempo NaN ride shared paths). Data ships valid; risk is future-edit drift. Add table-driven invalid-input tests in Phase 1. | P3 | Open |
| TD-016 | engine (purity guard blind spots) | LOW-006/007: `//` inside a string blanks line tail; `const {random} = Math` destructuring unmatched; circular-payload fallback returns raw ref. Tripwire, not sandbox. Consider `/\bMath\s*[{,}]/` + string-aware strip in Phase 1. | P4 | Open |
| TD-017 | engine (rng destructuring pin) | FIX-5 (int closure) has no test pin; add `const { range } = createRng(1); range(1,6)`. | P4 | Open |
| TD-018 | repo (pre-Phase-0, surfaced by pipeline) | CI blockers NOT caused by Phase 0: (a) check-links drift gate RED at HEAD — tests/*.test.ts count 362 vs README-pinned 355 (commit 63d2ca5 added tests without resync); (b) uncommitted src/components CSS-token WIP breaks FormPlanner + FormTemplatePicker tests (pin old bg-purple classes). Both must clear before ANY commit lands green. | P1 | Open |
| TD-019 | ui (export label stale) | PlaySessionRail.tsx:927 expectedSec = steps*(60/tempo)*4 now over-predicts the WAV ~3x (form-once export) and is meter-blind. Fix = detectFormPeriod(steps)*barSeconds(tempo,meter) via exported loopWav helper. GATED: file is the CSS-WIP author's — apply after that merge. | P2 | Open (gated) |
| TD-020 | theory (drift indexing seam) | barDriftShifts: per-BAR ramp array indexed by STEP index (theory.ts:322 + App.tsx:704); ramp is 4x compressed vs audio; form-once export captures only first pass of drift. Export==live for first pass (faithful) but the seam itself remains. | P3 | Open |
| TD-021 | export (small gaps) | (a) Entry-B filename lacks mode suffix; (b) empty path renders 1.25s silence — document contract; (c) wavMode/notesOverride passthrough at App.tsx call sites untested (mutation-untested class) — e2e candidate. | P4 | Open |
| TD-022 | data (catalog bar counts) | Ingested forms diverge from real-world: i-got-rhythm 28 bars, blue-bossa 17 (prime!), body-and-soul 21 vs canonical 32. Export honors declared form (correct behavior); the SONGS dict segmentation in ingest_standards.py needs review vs real changes. Also: 3 doubled-blues entries labeled b1..b24 are really 12-bar forms — consider relabel. | P3 | Open |
| TD-023 | phase-1 (AppMain extraction) | MEDIUM-001 reviewer: App.tsx body kept inline as 1900-line JSX fragment passed to <ModeGate>. Extract into a real exported component so future surfaces swap a JSX tag rather than rewrite a fragment. Per ADR-008 Phase 1.5 sequencing. | P3 | Open |
| TD-024 | phase-1 (ModeGate rehydration flash) | HIGH-002 reviewer: useState initializer runs synchronously but zustand persist rehydrates async, so first-render mode can flash etude before persisted compose/explore resolves. Fix via useSessionStore.persist.onFinishHydration. ~16ms visible to users with non-etude persisted mode. | P4 | Open |
| TD-025 | phase-1 (IdeaBar source pill wording) | LOW-001 reviewer: shipped as `from {idea.source}`; design wanted `from Etude bar 3` once bar number lands in Phase 3. Today Idea type has no bar number - acceptable until then. | P4 | Open |
| TD-026 | phase-1 (App.tsx keyboard handler comment) | LOW reviewer round 2: HIGH-001 guard's inline comment says "yield to Cmd/Ctrl/Alt + digit" but guard placement is broader (also affects Arrow/Space/M/[]+modifier). Update comment when the handler next changes. Benign side-effect actually fixes Cmd+Space Spotlight and Cmd+M minimize collisions. | P4 | Open |
| TD-027 | modes (popstate unwired) | ModeGate reads URL once at boot; browser back/forward between ?mode= links never switches surfaces (replaceState sync pushes no entries either). Wire popstate or document links-only. From ADR-011. | P3 | Open |
| TD-028 | tests (cheatsheet stale descriptions) | tests/keyboard-shortcuts.test.ts internal descriptions say "[": "tempo -5 BPM" - bindings moved to transpose in Phase 2. Unasserted strings; refresh next tests/-editable window. | P4 | Open |
| TD-029 | ui (reset chip semantics) | StageFrame reset chip displays soundingShift (global+exercise) but zeroes GLOBAL only - click looks like a no-op when exercise!=0. Retitle or reset both in a later pass. | P4 | Open |
| TD-030 | tests (App wiring coverage) | Untested by policy (no App unit tests): soundingShift sum derivation, onExportMidi semitones passthrough, queueMicrotask cycle dispatch. Mutation tests a-f confirmed UNCAUGHT (tester round 2). e2e candidates. | P3 | Open |
| TD-031 | ui (cycle display from negative offset) | advanceKeyCycle lands 0..11: cycling from manual -5 jumps display to +8 (octave-equivalent, design-specified). Display-only surprise; consider preserving sign region. | P4 | Open |
| TD-032 | slice-2 drive-bys | Three LOW notes from Phase 3 slice 1 round 2: (a) axis-progression DEFINITION says "major thirds or tritones - symmetric axis of Bartok" while corrected body says axis = minor thirds (one-line prose fix); (b) annotate.test.ts:92-94 comment half-describes the II7 fixture wrongly; (c) docs/PHASE-3-ETUDE.md sec 6 says "75 cases", actual matrix is 45 (historical design doc - add errata line or leave with ADR-014 note). | P4 | Open (slice 2) |
| TD-033 | etude (cosmetic debt, slice 2 review) | (a) etu-etu-<hash> double prefix in path ids + download filenames (canonicalId already carries kind); (b) MusicXML middle-segment tie start-before-stop (schema-valid, MuseScore-tolerant); (c) empty seed input coerces to 0 silently vs bars/tempo validation errors; (d) urlSyncPredicate reference-compare contract documented but unpinned (deep-equal mutation would pass); (e) e2e FIRST_PATH_TITLE hardcoded, no drift gate. | P4 | Open |
| TD-034 | etude (melody audio unwired) | Generated etude melody reaches notation + piano roll ONLY; melodyByStep (audio lane) is never seeded on accept - chords play, melody does not. Follow-up: subsample etude melody to melodyByStep strong beats on accept, or wire full playback. Documented in ETUDE-COMPOSER "Not yet". | P3 | Open |
| TD-035 | ui (legacy floor(step/4) on etude paths) | co-compose/live-score/melody-lane sites still use floor(activeStepIndex/STEPS_PER_BAR) - on one-step-per-bar etude paths these lag 4x (same class as the fixed activeBar bug, deliberately not touched in slice 2 scope). Audit + fix with the slice-3 annotation surfaces pass. | P3 | Open |
| TD-036 | docs | (a) docs/MODES.md title still "(PRD-001 Phases 1-2)"; (b) FUTURE_PLANNING Phase 8 row "Session sharing via URL | NOT STARTED" is untrue since Phases 1-2 + etude params - needs a refresh when Phase 8 is planned. | P4 | Open |
| TD-037 | pedagogy (dormant refs) | All 8 concepts ship references: null - drawer link path is fixture-pinned only. Populate curated refs on pedagogy review (do NOT author fake URLs). | P4 | Open |
| TD-038 | practice (accepted tails) | (a) stop()/setTempo mid-triplet: <=2 scheduled partners fire after stop (~0.5s tail) - gain-cancel or accept; (b) default metronome level +4dB vs legacy (0.8 on metronomeGain vs old masterGain 0.5) - manual audio smoke owed; (c) gate-routed e2e coverage is header+Space+Escape only of 7 sites (rail/mobile/MIDI/commit/audition uncaught bypasses). | P4 | Open |
| TD-039 | docs (stale rows) | FUTURE_PLANNING Phase 6 "Concept registry (>=8) NOT STARTED" still stale (shipped slice 1) - docs sweep round. | P4 | Open |
| TD-040 | etude (generator minor-mode defect) | Phase 3 jazz style templates emit major-functional harmony regardless of requested mode - a "Bb minor" etude resolves Bbmaj9. SEPARATE fast-follow: the fix changes etude output, invalidating determinism goldens, so it needs its own cycle (generator fix + golden refresh + count-pin audit together). Surfaced by the Phase 4 S1 tester's KS corpus work; recorded in docs/PHASE-4-COMPOSE.md fix-round deferrals. | P2 | Open |
| TD-041 | compose (melody gap absorption) | Synthesized top line absorbs silence into note tails - no rests ever emitted. S2 preview UX note: do not render a held-across-gap note as "sustain" without a caveat (or re-extract with rest support). Recorded in docs/PHASE-4-COMPOSE.md fix-round deferrals. | P4 | Open |
| TD-042 | compose (slice 2 LOWs) | Bundle from S2 review/test rounds: (a) single-source the H1 allowed-agreement set from key.ts (clamp states it in 2 places); (b) analyzeFull stays true if full-window re-run hits internal arm (LOW-003); (c) restore double-analyzes ~2x p95 (LOW-004); (d) roll caption hardcodes "first 4:00" vs configurable window (LOW-005); (e) readMidiFile "Could not read the file" arm dead via surface pre-read; (f) clamp01(NaN)=NaN (unreachable via normalize guards); (g) dead control line chordInput.test.ts:129; (h) loaded-state drop not busy-gated (double-drop last-wins); (i) eslint-disable comment in repo w/o eslint + composeFixtures.tsx has no JSX. | P4 | Open |
| TD-043 | compose (S3/S4 integration pins owed) | CLOSED by Phase 4 Slice 4 (D79): withTempoOverride/withTimeSignatureOverride at the effectiveProject choke point - preview/mixer/WAV/MIDI all inherit the overrides (e2e legs 2/3 pin BOTH arms end-to-end); the D51 round-trip was SUPERSEDED by D80's keySig INSERTION (the golden test asserts EQUALITY, not degradation - the @tonejs encoder path is bypassed entirely); S3's per-bar slots.length reads shipped in S3 itself. | P3 | Closed - 2026-09-24, PRD-001 Phase 4 S4 |
| TD-044 | compose (fingerprint omits bassPc) | CLOSED by Phase 4 Slice 4 (D89): chart paste makes slash-bass cells REACHABLE ("C/E", "G7/B" via the shared grammar), so the watch-item condition became TRUE; gridFingerprint now serializes "rootPc.qualitySymbol[/bassPc]" (sensitivity + stability pins in accompany.test.ts). No migration: results are in-memory-only (D73). | P4 | Closed - 2026-09-24, PRD-001 Phase 4 S4 |
| TD-045 | compose (S3 fast-follow LOWs) | (a) CLOSED by Phase 4 Slice 4 (D90): voiceEnvelopeTimes pure helper clamps endT = max(rawEnd, attack+decay) - release ramp can never precede the hold (monotonicity pins over durs x all 4 voices incl. the 0.001s stab in composeVoices.test.ts). Remaining: (b) annotation targets key barIdx vs region.bar (latent if window ever starts mid-file); (c) double-click during rendering = wasted 2nd render (harmless); (d) charleston label "dotted quarter" vs authored span 2 (data-only retune); (e) bass "seventh" token dead/mislabeled on 6-chords; (f) 8 of 14 patterns unreferenced by shipped profiles (by design; needs style-pack or per-pattern control to surface); (g) 'comping' concept registered but no annotation emits it yet. | P4 | Open |
| TD-046 | compose (stems ZIP) | REQ-COMP-42 stems export DEFERRED per parent X10 ("42 deferred - P2 honesty"). D77's per-group baked buffers make it NEARLY FREE: renderMixGroups already yields the 4 AudioBuffers; zip = midiBatchExport's fflate zipSync precedent + encodeWav per group. Fast-follow, not a redesign. | P2 | Open |
| TD-047 | url (/play route) | REQ-IO-52 minimal /play route DEFERRED (D86): no router exists, the app is single-route, and a top-level surface for a P2 the PRD never designed is scope creep. Revisit with a real routing decision. | P2 | Open |
| TD-048 | ui (global Space in Compose) | PRE-EXISTING quirk, NOT touched by S4 (D87: zero key-handler edits; the compose mixer is button-only): App's window keydown binds Space to practice playback in ALL modes, so Space in Compose mode starts hidden practice audio. Fix = one mode-gate in the global handler (Phase 5 hygiene). | P3 | Open |
| TD-046 | compose (S4 deferred) | Stems ZIP (REQ-COMP-42 P2) deferred per parent X10; D77 architecture makes it nearly free later (per-group baked buffers already exist). | P3 | Open |
| TD-047 | compose (S4 deferred) | /play route (REQ-IO-52 P2) deferred - no router exists, app is single-route. | P4 | Open |
| TD-048 | app (pre-existing quirk) | Space globally bound to practice playback in ALL modes incl. compose (pre-dates S4); no App key-handler edits per S4 scope. Revisit when compose playback routing is designed. | P4 | Open |
| TD-EXP-MOD | explore (REQ-EXP-14 deferred) | Modulate (key-change bridge progression) has no pivot-key primitive; stub + deferral test ship, no UI button. Needs key-change analysis discipline before any UI. | P3 | Open |
| TD-EXP-LITERAL | explore (forbidden-pattern guard) | Send-to-Etude must remain constraints-carry, never literal degree transplant (symbol/numeral grammars disjoint). Guard the seam if card shapes evolve. | P3 | Open |
| TD-EXP-SLOT | explore (bookkeeping) | Vary/voicelead attempt namespaces must stay per-op (LOW-002 slip fixed); card slot bookkeeping follow-ups ride future op work. | P4 | Open |
| TD-EXP-MODAL-Q | explore (coverage, deliberate) | Quality-borrow detector on diatonic roots (iv7/III7 coverage cut when modal pools reduced to root-residual-detectable set). New detector = fast-follow. | P3 | Open |
| TD-PED-6-REST | pedagogy (scoped deferral) | Right-click/long-press concept access shipped for AnalysisCard chord cells only; roman-numeral hosts (LiveScoreDisplay, bar strip, roll labels) live in dirty-11 or lack clean anchors. Wire when those hosts are editable. | P3 | Open |
| TD-PED-SUMMARY | pedagogy (UI carve-out) | REQ-PED-41 summary math + storage ship (log.ts summarize, caps, UTC-day) but NO on-screen panel renders it (minutes-per-day, concept counts, rolling accuracy). Small surface: a Progress section or drawer tab consuming summarize(). | P3 | Open |
| TD-CI-E2E-FLAKE | ci (infra) | Full-suite playwright runs show transient ERR_CONNECTION_REFUSED / trace-teardown ENOENT on compose/etude specs (webServer startup race, single-worker long runs); all pass in isolation and on re-run. Investigate playwright webServer reuse or per-spec server. Not product. | P3 | Open |
