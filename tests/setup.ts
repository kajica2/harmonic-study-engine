/**
 * Vitest setup. Runs before every test file.
 *
 * Currently a no-op — kept as a hook for future setup (e.g.
 * jest-dom matchers, fetch polyfills, AudioContext mocks for the
 * loopWav tests).
 */